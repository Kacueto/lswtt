import React from 'react';
import './Footer.css';

const Footer = () => {
    return (
        <footer>
            <p>Este es el footer de la página.</p>
        </footer>
    );
};

export default Footer;